# Upstream backend
upstream npanel_backend {
    server 127.0.0.1:3000;
}

# Upstream frontend
upstream npanel_frontend {
    server 127.0.0.1:3001;
}

# Shared proxy configuration
map $request_uri $admin_redirect {
    ~*^/$ /admin;
    default $request_uri;
}

# ============================================
# Main Port 8080 (HTTP) - Mixed routing
# ============================================
server {
    listen 8080;
    server_name localhost;

    # API endpoints - route to backend
    location /api {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /v1 {
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Admin panel
    location /admin {
        proxy_pass http://npanel_frontend/admin;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Customer panel
    location /customer {
        proxy_pass http://npanel_frontend/customer;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Login page
    location /login {
        proxy_pass http://npanel_frontend/login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Root - redirect to admin by default
    location = / {
        return 301 /admin;
    }

    # Static files and other frontend routes
    location / {
        proxy_pass http://npanel_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# Port 2086 (HTTP) - Admin Only
# ============================================
server {
    listen 2086;
    listen [::]:2086;
    server_name localhost;

    # API endpoints - route to backend
    location /api {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /v1 {
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Root - redirect to admin
    location = / {
        return 301 /admin;
    }

    # Admin panel
    location /admin {
        proxy_pass http://npanel_frontend/admin;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Deny customer access on admin port
    location /customer {
        return 403;
    }

    # Static files and frontend routes
    location / {
        proxy_pass http://npanel_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# Port 2087 (HTTPS) - Admin Only
# ============================================
server {
    listen 2087 ssl;
    listen [::]:2087 ssl;
    server_name localhost;

    # SSL Configuration (self-signed certificates for development)
    ssl_certificate /etc/ssl/certs/npanel-admin.crt;
    ssl_certificate_key /etc/ssl/private/npanel-admin.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # API endpoints - route to backend
    location /api {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /v1 {
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    # Root - redirect to admin
    location = / {
        return 301 /admin;
    }

    # Admin panel
    location /admin {
        proxy_pass http://npanel_frontend/admin;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    # Deny customer access on admin port
    location /customer {
        return 403;
    }

    # Static files and frontend routes
    location / {
        proxy_pass http://npanel_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# ============================================
# Port 2082 (HTTP) - Customer Only
# ============================================
server {
    listen 2082;
    listen [::]:2082;
    server_name localhost;

    # API endpoints - route to backend
    location /api {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /v1 {
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Root - redirect to customer
    location = / {
        return 301 /customer;
    }

    # Customer panel
    location /customer {
        proxy_pass http://npanel_frontend/customer;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Deny admin access on customer port
    location /admin {
        return 403;
    }

    # Login page
    location /login {
        proxy_pass http://npanel_frontend/login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static files and frontend routes
    location / {
        proxy_pass http://npanel_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================
# Port 2083 (HTTPS) - Customer Only
# ============================================
server {
    listen 2083 ssl;
    listen [::]:2083 ssl;
    server_name localhost;

    # SSL Configuration (self-signed certificates for development)
    ssl_certificate /etc/ssl/certs/npanel-customer.crt;
    ssl_certificate_key /etc/ssl/private/npanel-customer.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # API endpoints - route to backend
    location /api {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /v1 {
        proxy_pass http://npanel_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    # Root - redirect to customer
    location = / {
        return 301 /customer;
    }

    # Customer panel
    location /customer {
        proxy_pass http://npanel_frontend/customer;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    # Deny admin access on customer port
    location /admin {
        return 403;
    }

    # Login page
    location /login {
        proxy_pass http://npanel_frontend/login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    # Static files and frontend routes
    location / {
        proxy_pass http://npanel_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
